# Jarvis AI Backend Engine

The backend control plane for Jarvis AI Automation system.

## Setup & Running

```bash
# Install dependencies
pip install -r requirements.txt

# Run development server
python -m app.main

# Run tests
pytest
```
